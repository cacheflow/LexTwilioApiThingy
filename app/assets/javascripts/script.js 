$(document).on 'page:change', function() {
 if (window._gaq != null) {
    return_gaq.push(['trackPageview']);
    }else if (window.pageTracker != null){
      return pageTracker.trackPageview();
      }
      });
